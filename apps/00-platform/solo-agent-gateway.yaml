apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: solo-agent-gateway
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "3"
spec:
  project: default
  source:
    repoURL: https://github.com/rorobig/homelab-gitops.git
    targetRevision: main
    path: platform/networking/solo-agent-gateway
  destination:
    server: https://kubernetes.default.svc
    namespace: solo-agent-gateway
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
